spring:
  application:
    name: authorization-server
---
spring:
  profiles: dev
  cloud:
    config:
      uri: https://betting-app-config-server.herokuapp.com/
      username: ${CONFIG_USER}
      password: ${CONFIG_PASSWORD}